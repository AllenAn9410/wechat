<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.anx.serve.mapper.UserInfoMapper">


        <!-- 通用查询映射结果 -->
        <resultMap id="BaseResultMap" type="cn.anx.serve.entity.UserInfo">
                    <id column="id" property="id" />
                    <result column="user_name" property="userName" />
                    <result column="user_code" property="userCode" />
        </resultMap>

        <!-- 通用查询映射结果 -->
        <resultMap id="BaseResultJoinMap" type="cn.anx.serve.entity.join.UserInfoJoinServices">
                <id column="id" property="id" />
                <result column="user_name" property="userName" />
                <result column="user_code" property="userCode" />
        </resultMap>

        <!-- 通用查询结果列 -->
        <sql id="BaseColumnList">
                `user_info`.id,
                `user_info`.user_name,
                `user_code`.user_code
        </sql>

        <!-- 通用查询结果列 -->
        <sql id="BaseColumnListJoin">
                `user_info`.id,
                `user_info`.user_name,
                `user_code`.user_code
        </sql>


    <select id="selectUserInfoJoinServices" resultMap="BaseResultJoinMap">
        select
            <include refid="BaseColumnList" />
        from  user_info `user_info`
        <where>
            ${ew.sqlSegment}
        </where>
    </select>


</mapper>
